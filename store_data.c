void store_keystroke_event(int key_code, char event_type, long long timestamp) {
    // logs all keystroke events in sequential order within memory
}


void export_to_csv(const char *csv_file_name) {
    // calls create_csv_file() and stores the returned name into a variable

    // takes keystroke events in memory, and appends them to the CSV file
}

char create_csv_file() {
    // create CSV file with configuration naming convention

    // return the CSV file name
}

void cleanup_storage() {
    // cleans up all allocated storage
}







































